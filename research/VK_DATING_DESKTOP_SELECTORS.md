# VK Dating Desktop Selectors

> Автоматически собрано через click_recorder.py  
> Дата: 2026-01-09

## 🎯 Основные действия (Анкеты)

### Кнопки реакций
Используют `aria-label` и `data-reaction` — **самые надёжные селекторы!**

| Действие | Селектор | Горячая клавиша |
|----------|----------|-----------------|
| **Лайк** | `[aria-label="like"]` или `[data-reaction="like"]` | `.` (точка) |
| **Дизлайк** | `[aria-label="dislike"]` или `[data-reaction="dislike"]` | `,` (запятая) |
| **Суперлайк** | `[aria-label="super-like"]` или `[data-reaction="super-like"]` | — |

### Навигация по фото
| Действие | Селектор | Горячая клавиша |
|----------|----------|-----------------|
| Следующее фото | `.OAf1LKm6.Mq6KKuwQ` | `→` (ArrowRight) |
| Предыдущее фото | `.OAf1LKm6.wsUQAjKL` | `←` (ArrowLeft) |

---

## 📑 Вкладки навигации

| Вкладка | Селектор |
|---------|----------|
| Анкеты | `.vkuiTabsItem__label:has-text("Анкеты")` |
| Лайки | `.vkuiTabsItem__label:has-text("Лайки")` |
| Чаты | `.vkuiTabsItem__label:has-text("Чаты")` |
| Профиль | `.vkuiTabsItem__label:has-text("Профиль")` |

**Playwright код:**
```python
await frame.locator('span.vkuiTabsItem__label', has_text='Лайки').click()
```

---

## 💬 Чаты и сообщения

| Элемент | Селектор |
|---------|----------|
| Поле ввода сообщения | `.vkuiWriteBar__textarea` |
| Кнопка отправки | `.Gk4oo6P9` или `button[aria-label="send"]` |
| Список чатов (элемент) | `.MWrUXww6` |
| Мэтчи (горизонтальный список) | `.vkuiHorizontalCell__body` |

---

## 👤 Данные профиля (карточка анкеты)

| Данные | Селектор |
|--------|----------|
| Имя + возраст | `.j2wk1ydI` (заголовок карточки) |
| Био описание | `.QLecl7_H` |
| Раздел "Я ищу" | `.Zk9UNqxx` |
| Образование | `.vkuiMiniInfoCell__content` (с иконкой 🎓) |
| Работа | `.vkuiMiniInfoCell__content` (с иконкой 💼) |
| Интересы | `.vkuiHeader__contentIn:has-text("Интересы")` |

---

## ⚙️ Фильтры

| Элемент | Селектор |
|---------|----------|
| Кнопка фильтра | `.gPyaghhN` или `button[aria-label="filter"]` |
| Слайдер возраста | `.vkuiSliderThumb__host` |
| Слайдер радиуса | `.vkuiSliderThumb__host` |
| Цель знакомства | `.RWxPNm3S` |
| Кнопка "Применить" | `button:has-text("Применить")` |
| Кнопка "Сохранить" | `button:has-text("Сохранить")` |

---

## 🔧 Настройки профиля

| Элемент | Селектор |
|---------|----------|
| Кнопка настроек | `.vkuiInternalTappable` (иконка ⚙️) |
| Поле БИО | `.vkuiUnstyledTextField__host` или `textarea.vkuiTextarea__el` |
| Перетаскивание фото | `.I7ZkiYCm` |
| Кнопка "Готово" | `button:has-text("Готово")` |
| Кнопка "Назад" | `.vkuiPanelHeaderButton__backVkcom` |

---

## 🎮 Рекомендуемый подход

### 1. Горячие клавиши (самый надёжный)
```python
# Лайк
await page.keyboard.press('.')
# или
await page.keyboard.press('Period')

# Дизлайк  
await page.keyboard.press(',')
# или
await page.keyboard.press('Comma')

# Навигация по фото
await page.keyboard.press('ArrowRight')
await page.keyboard.press('ArrowLeft')
```

### 2. Aria-label селекторы
```python
# Получаем iframe с mini-app
frame = page.frame_locator('iframe').first

# Лайк
await frame.locator('[aria-label="like"]').click()

# Дизлайк
await frame.locator('[aria-label="dislike"]').click()

# Суперлайк
await frame.locator('[aria-label="super-like"]').click()
```

### 3. Data-атрибуты
```python
await frame.locator('[data-reaction="like"]').click()
await frame.locator('[data-reaction="dislike"]').click()
```

---

## ⚠️ Важные замечания

1. **VK Dating работает внутри iframe** — нужно сначала получить frame
2. **Классы типа `DvDUWVqV` — обфусцированы** и могут меняться при обновлениях
3. **aria-label и data-reaction — стабильные** селекторы
4. **Горячие клавиши работают без iframe** — самый надёжный метод

---

## 📁 Структура приложения

```
vk.com/dating
└── iframe (prod-app7058363-*.pages-ac.vk-apps.com)
    ├── Вкладка "Анкеты" (/)
    │   ├── Карточка профиля
    │   │   ├── Фото (свайп влево/вправо)
    │   │   ├── Имя, возраст
    │   │   └── Био, интересы
    │   └── Кнопки реакций
    │       ├── Дизлайк (слева)
    │       ├── Суперлайк (центр)
    │       └── Лайк (справа)
    ├── Вкладка "Лайки"
    │   └── Список тех, кто лайкнул
    ├── Вкладка "Чаты"
    │   ├── Мэтчи (горизонтальный список)
    │   └── Переписки (вертикальный список)
    └── Вкладка "Профиль"
        ├── Превью профиля
        └── Настройки
```
